interface:
  display_name: "To Spec"
  short_description: "Turn current context into a product spec"
  default_prompt: "Use $to-spec to turn this conversation into a product spec."

policy:
  allow_implicit_invocation: false
