interface:
  display_name: "Zoom Out"
  short_description: "Map unfamiliar code at a higher level"
  default_prompt: "Use $zoom-out to explain how this area of code fits into the bigger picture."

policy:
  allow_implicit_invocation: false
